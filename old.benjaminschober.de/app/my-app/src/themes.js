import theme from 'styled-theming';

export const backgroundColor = theme("theme", {
    light: "#fff",
    dark: "#181818",
});

export const textColor = theme("theme", {
    light: "#000",
    dark: "#fff",
});

export const navbarTextColor = theme("theme", {
    light: "#000",
    dark: "#fff",
});

export const navbarTextHover = theme("theme", {
    light: "#565656",
    dark: "#e7e7e7"
});